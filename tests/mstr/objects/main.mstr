(module 'tests 'objects 'main)
(state (start)
  (steps
    (let foo empty-object)
    (let profile 'profile)
    (let name 'name)
    (let age 'age)
    (let foo profile name "Ada")
    (set foo profile age 38)
    (transition end (list (get foo profile name) (get foo profile age)))))
